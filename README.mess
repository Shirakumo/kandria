# About Leaf
This is a game project.

## Repository note.
The project uses "Git LFS"(https://git-lfs.github.com/). Please ensure git-lfs is installed. After cloning the repository, run ``git lfs install``, ``git lfs fetch`` and ``git lfs checkout`` to properly set up the data files and tracking.

## Public Feedback
Changes that are realised thanks to public feedback will be noted in the "CHANGES.mess"(link CHANGES.mess) file.
